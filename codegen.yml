schema: .graphql/schema.graphql

generates:
  ./src/libs/codegen.ts:
    documents:
      - 'src/libs/**/*.{ts,tsx}'
      - '!src/libs/**/*.codegen.ts'
    plugins:
      - typescript
      - typescript-operations
      - typescript-urql
    config:
      scalars:
        DateTime: string
